pipeline {
    agent any
    stages {
      stage ('Compile Stage') {
        steps {
          sh 'mvn clean comple'
        }
      
      }

      Stage ('Testing') {
       steps{
         sh 'mvn test'
       }
      
      }

       Stage ('Deploy') {
       steps{
         sh 'mvn deply
       }
      
      }
 }
}
  
  
